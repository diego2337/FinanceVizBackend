version: '3.8'

services:
  finance-viz-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    volumes:
      - ./src/main/resources:/app/src/main/resources # Optional for local resource mapping
